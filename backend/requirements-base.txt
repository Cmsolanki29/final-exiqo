# Python 3.13–friendly pins (numpy/sklearn wheels). Do not replace with older tutorial pins:
# downgrading FastAPI/pydantic/numpy breaks Phase 2 routes and the ML stack.
#
# Core SmartSpend API (FastAPI, Postgres, sklearn anomaly path, OpenAI-compatible clients).
# Password length: `utils/auth.py` (_bcrypt_plaintext) + passlib bcrypt.
# passlib 1.7.4 breaks against bcrypt>=4.1 — pin bcrypt 4.0.x below.
#
# Install into a project venv — see requirements.txt header.
# Windows Store Python: avoid `pip install --user` for the full tree; use `python -m venv .venv`.
fastapi==0.115.0
uvicorn==0.30.6
psycopg2-binary==2.9.11
python-dotenv==1.0.1
scikit-learn==1.6.1
numpy==2.2.2
pandas==2.2.3
openai==1.51.0
groq>=0.11
google-generativeai>=0.8.0
pydantic==2.9.2
python-multipart==0.0.12
python-jose[cryptography]==3.3.0
bcrypt==4.0.1
passlib[bcrypt]==1.7.4
email-validator==2.2.0
httpx>=0.26
pdfplumber>=0.11.0
PyMuPDF>=1.24.0
pytesseract>=0.3.10
Pillow>=10.0.0
chardet>=5.2.0
openpyxl>=3.1.0
xlrd>=2.0.1
python-dateutil>=2.9.0
# Trip Planner — Travel Intelligence MCP (stdio). Pin starlette for FastAPI 0.115.
mcp>=1.0.0,<2
starlette>=0.37.2,<0.39.0
