# Phase 1–8 risk engine + observability + tests (no PyTorch — see requirements-torch.txt).
asyncpg>=0.29
redis[hiredis]>=5.0
xgboost>=2.0
mlflow>=2.10
shap>=0.44
prometheus-client>=0.19
structlog>=24.1
pydantic-settings>=2.0
apscheduler>=3.10
circuitbreaker>=2.0
pytest-asyncio>=0.23
pyod>=1.1
groq>=0.11
