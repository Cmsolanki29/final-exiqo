# Full install (CI / Linux / dev machines with long-path or short-path venvs):
#   pip install -r requirements.txt
#
# Windows + Microsoft Store Python: if the full install fails with OSError / long paths
# during setuptools or torch, use a project venv first:
#   cd backend
#   python -m venv .venv
#   .\.venv\Scripts\Activate.ps1
#   python -m pip install -U pip wheel
#   pip install -r requirements-base.txt -r requirements-ml-risk.txt
# Then optionally (needs long paths or clean venv):
#   pip install -r requirements-torch.txt
#
# torch_geometric — install separately; see PHASE_9_TO_12_LOG.md if present.
-r requirements-base.txt
-r requirements-ml-risk.txt
-r requirements-torch.txt
